export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum UserRole {
  User,
  Admin
}
export enum tokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerificationToken
}
